import { Box } from "@chakra-ui/react";

export const Stepper = () => {
  return (
    <Box className="!text-white flex items-center justify-center font-bold text-4xl relative -top-10 bg-green-600">
      ADD STEPPER HERE - code if from scratch
    </Box>
  );
};
